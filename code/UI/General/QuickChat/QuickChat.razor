@using Sandbox;
@using TTT;

@namespace TTT.UI

@if (!Game.LocalPawn.IsAlive() || !IsShowing)
{
    return;
}

<root>
    <QuickChatEntry Index=1 Target=@_target Prefix="I'm with" Suffix="." />
    <QuickChatEntry Index=2 Target=@_target Prefix="I see" Suffix="." />
    <QuickChatEntry Index=3 Prefix="Yes." />
    <QuickChatEntry Index=4 Prefix="No." />
    <QuickChatEntry Index=5 Target=@_target Suffix="is a Traitor!" />
    <QuickChatEntry Index=6 Target=@_target Suffix="acts suspicious." />
    <QuickChatEntry Index=7 Target=@_target Suffix="is Innocent." />
    <QuickChatEntry Index=8 Prefix="Help!" />
    <QuickChatEntry Index=9 Prefix="Anyone still alive?" />
</root>

<style>
    QuickChat {
        position: absolute;
        left: 25px;
        top: 50%;
        transform: translateY(-50%);
        flex-direction: column;
        z-index: 101;
        font-size: 18px;
        align-items: flex-start;

        .index {
            font-weight: bold;
        }
    }
</style>