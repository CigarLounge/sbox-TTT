@using Sandbox;
@using TTT;

@namespace TTT.UI

<root class="opacity-heavy text-shadow">
    @if (!CameraMode.Target.IsValid() || !CameraMode.Target.IsAlive())
        return;

    @if (CameraMode.Target.Role is not NoneRole)
    {
        <div class="role-container" style="background-color: @CameraMode.Target.Role.Color.Hex">
            <text>
                <text>@CameraMode.Target.Role.Title</text>
            </text>
        </div>
    }

    <div @ref=HealthContainer
        class="health-container background-color-gradient @(CameraMode.Target.Role is not NoneRole ? "left-flat" : "")">
        <text>✚ @CameraMode.Target.Health.CeilToInt()</text>
    </div>
</root>

<style>
    PlayerInfo {
        position: absolute;
        bottom: 25px;
        left: 25px;

        .role-container {
            padding: 2px 10px;
            border-radius: 4 0 0 4;
            justify-content: center;
            font-size: 30px;
            font-weight: bold;
        }

        .health-container {
            width: 115px;
            padding: 2px 10px;
            border-radius: 4px;
            justify-content: center;
            font-size: 30px;
            font-weight: bold;

            &.hit {
                color: rgb(255, 189, 189);
            }

            &.left-flat {
                border-radius: 0 4 4 0;
            }
        }
    }
</style>
